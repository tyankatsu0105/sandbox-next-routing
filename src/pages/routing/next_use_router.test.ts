import { describe, it, expectTypeOf, expect } from "vitest";

import * as Feature from "./next_use_router";

describe("next_use_router", () => {
  describe("getTypedQuery", () => {
    it("返却値のqueryは、useRouterのqueryプロパティと同じである", () => {});
  });
});
